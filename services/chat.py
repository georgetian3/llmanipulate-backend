from models.models import LLMPrompt, LLMResponse


async def get_llm_response(prompt: LLMPrompt) -> LLMResponse:
    # TODO: get and return LLM response
    return LLMResponse(response="Sample LLM Response")
